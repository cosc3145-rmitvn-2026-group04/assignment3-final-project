import pygame
import sys, random
from pygame.math import Vector2

def main():
    dt: float = clock.tick(FPS) / 1000.0
    running = True
    while running:
        pass
    
    pygame.quit()
    sys.exit(0)
    
if __name__ == "__main__":
    main()
